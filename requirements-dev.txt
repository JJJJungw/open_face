# 테스트만 돌리는 데 필요한 최소 집합.
# torch/torchvision 은 일부러 뺐다 — 파이프라인 테스트는 가짜 검출기를 주입하므로
# 2GB 짜리 torch 없이도 전 구간을 검증한다. (tests/conftest.py 참고)
opencv-python>=4.7
numpy>=1.23
supervision>=0.18,<0.30
pytest>=7.4
fastapi>=0.110
python-multipart>=0.0.9
httpx>=0.27
