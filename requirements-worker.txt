# MSA 워커 전용 — 잡 페이로드 하나로 도는 컨테이너. 코어에 더해서 설치한다.
#   pip install -r requirements.txt -r requirements-worker.txt
#
# **fastapi 도 boto3 도 없다.** 이 컨테이너는 우리 HTTP 서버를 띄우지 않고,
# 버킷에 자기 열쇠로 붙지도 않는다 — 서명된 URL 하나로만 드나든다. 이미지에
# 비밀이 안 들어가는 이유가 여기 있다(docs/integration/rebornstudio.md).
#
# 단독 운영(웹 화면)은 requirements-serve.txt 쪽이다. 둘은 같이 깔아도 되지만
# 워커 이미지에 서버를 넣을 이유는 없다.
httpx>=0.27

# 큐 껍데기(msa/). 우리를 호출하는 사람이 없으므로 HTTP 서버 대신 이게 필요하다 —
# Redis 큐를 지켜보다가 잡을 스스로 꺼내 온다. redis 엑스트라가 브로커 드라이버다.
celery[redis]>=5.3
