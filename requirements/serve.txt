# HTTP API + 웹 UI 전용. 코어(requirements/base.txt)에 더해서 설치한다.
#   pip install -r requirements/base.txt -r requirements/serve.txt
fastapi>=0.110
uvicorn[standard]>=0.27

# S3 연동 (FA_S3_BUCKET 을 쓸 때만 필요)
boto3>=1.34
